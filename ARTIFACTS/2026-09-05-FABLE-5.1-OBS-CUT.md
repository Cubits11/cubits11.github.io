# OBSERVATION CUT — BELLS denominators and provenance

Written 2026-09-05 against working tree of branch `claude/mc-005-selection-regret`
(HEAD `f9b24e2`, uncommitted owner changes present and untouched). Status
vocabulary: OBSERVED (bytes opened or command executed in this run) · DERIVED
(arithmetic on OBSERVED material) · UNKNOWN. This packet registers no claim,
edits no registry, spends no money, and calls no live guard API.

Path note: the requested location `artifacts/…` and the tracked directory
`ARTIFACTS/` are one directory on this APFS volume (same inode); git tracks
the upper-case name, so this file lives at `ARTIFACTS/2026-09-05-FABLE-5.1-OBS-CUT.md`.

## VERDICT

The published BELLS joint number (9 of 82 all-miss, MC-002) is a counting fact
about a bounded 2025 snapshot: the 82 harmful verdicts for Lakera Guard, NeMo
Guardrails and LLM Guard are byte-identical to the 2025-02-18 population file
upstream, the upstream FAQ dates all supervisor measurements to January–February
2025, no per-item release newer than 2025-03-22 exists, and no public sentence
in this repository asserts current behaviour of any supervisor — so Cut 1 has no
decision to change.

## CUT 0 — denominator / provenance table

Regenerate: `python3 scripts/bells_denominators.py` (network, hash-asserted,
nothing redistributed) or `--cache DIR` offline. Exit 0 this run.

### Per-item files (upstream `CentreSecuriteIA/bells_leaderboard`, all pinned by sha256)

| # | commit (date, subject) | source file | total n | harmful / benign / borderline | harmful categories | supervisor columns present | constant within file | varying | selection rule | denominator type | claims depending on it |
|---|---|---|---|---|---|---|---|---|---|---|---|
| A | `0fc3d6d3` (2025-02-18, "[WIP] v0") | `data/non_adversarial_prompts.csv` sha `b3d5553510f0…` | 1080 | 360 / 360 / 360 | 12 (incl. Miscellaneous) | lakera_guard, prompt_guard, langkit, nemo, llm_guard (no frontier columns) | `prompt_guard=1`, `langkit=1` (placeholders, not verdicts) | lakera_guard, nemo, llm_guard, harm_level, source, category | full pool at v0; rule for the pool itself not documented | full file (pool) | none in this repository |
| B | `d6ebd0e5` (2025-02-18, "Add frontier models to playground") | same path, sha `a8d2e4ea69b3…` | **1077** | 358 / 359 / 360 | 12 (incl. Miscellaneous) | five specialized + gpt-4, claude-3-sonnet-20240229, gemini-pro, mistral-large-latest | `prompt_guard=1`, `langkit=1` (placeholders) | lakera_guard, nemo, llm_guard, four frontier columns | 3 rows fewer than A; rule not documented | full file (pool) | none; encountered only via the prompt's "released file 1077" |
| C | `077555d9` (2025-02-19, "smaller dataset for playground") | same path, sha `7fa0fbf5885e…` | 174 | 86 / 50 / 38 | 12 | as B | `prompt_guard=1`, `langkit=1` | rest | **SELECTION RULE: UNKNOWN** (commit subject only: "smaller dataset for playground") | selected subset | none |
| D | `00b42bfd` (2025-02-21, "New data, new results interpretation") | same path, sha `52ca9ab8eb62…` | 174 | 86 / 50 / 38 | 12 | as C, claude column renamed `claude-3-5-sonnet-20241022` | none (Prompt Guard and LangKit now carry real 0/1 values) | all verdict columns | SELECTION RULE: UNKNOWN | selected subset | none |
| E | `ffe88ccb` (2025-02-23, "new playground data") | same path, sha `d93f9fe1d1a1…` | 174 | 86 / 50 / 38 | 12 | as D | `llm_guard=0` | rest | SELECTION RULE: UNKNOWN | selected subset | none |
| F | `b20aeed5` (2025-03-22, "new results") = **`507566c5` (2025-07-08, default-branch head)** | same path, sha `791dd4b0a168f2eb…03f57c3` | **170** | **82 / 50 / 38** | 11 (Miscellaneous absent) | five specialized + gpt-4, claude-3-5-sonnet-20241022, gemini-1.5-pro-latest, mistral-large-latest, deepseek-ai/DeepSeek-V3, grok-2-latest | none over 170 rows; within the 82 harmful rows `llm_guard=0` | all eleven verdict columns | **SELECTION RULE: UNKNOWN**. Only 74–76 of the 170 questions appear in C–E, so F is a fresh draw from pool B, not a trimming of C. All 170 questions are in A and B with identical `harm_level`. | selected subset (author-selected; rule unstated) | MC-002 (every `expected` count), MC-003 (`{0…12}/82`, leave-one-out), MC-005 at HEAD (n = 82), census row `bells-misuse-2025` (`item_level_outcomes_released`), dossier `bells-misuse-2025`, disclosure page, two `/answers/` pages, `/try/` TRY-B, film theses in `films/SLATE.md` |
| G | `507566c5` | `data/adversarial_prompts.csv` sha `32fe8663621a…` | 8 | 8 / 0 / 0 | 1 (all Miscellaneous) | eleven verdict columns | `harm_level`, `category`, `deepseek-ai/DeepSeek-V3=0` | rest | SELECTION RULE: UNKNOWN | selected subset | 12-WEEK-PROGRAM.md ("8 adversarial rows released"); census row ("plus 8 adversarial prompts") |
| H | `8a974123`→`507566c5` | `data/metacognitive_results.csv` sha `66a9f09c60d3…` | 612 rows = 102 prompts × 6 models | ground_truth 504 harmful / 108 not_harmful rows; 19 non-adversarial prompts | — | none of the five supervisors; frontier models only, with **newer versions** (`claude-3-7-sonnet-20250219`, `gemini-2.5-pro-exp-03-25`) | `model` set | responses | UNKNOWN | selected subset | none |

### Aggregates the leaderboard actually consumes

`leaderboard.js` and `recommendation.js` read `data/safeguard_evaluation_results.csv`;
`leaderboard_confidence.js` additionally reads `…_confidence.csv` and `…_llama.csv`;
`playground.js` reads files F and G. (`scripts.js` references six `/data/*_jailbreaks.csv`
paths that do not exist in the tree.)

| commit | aggregate file | safeguards | benign non-adv | borderline non-adv | harmful non-adv | benign jailbreaks | borderline jailbreaks | harmful jailbreaks |
|---|---|---|---|---|---|---|---|---|
| `0fc3d6d3` | `safeguard_evaluation_results.csv` sha `06eeb0251e5b…` | 9 | 359 | 360 | 358 | 553 | 529 | 3448 |
| `00b42bfd` | same, sha `d906e69c1fd8…` | 9 | 359 | 360 | 358 | 431 | 407 | 3327 |
| `dde32a3d` (2025-02-23, "final results") | same, sha `6c133bea489b…` | 9 | **353** | **360** | **328** | 431 | 407 | not a simple fraction ≤ 40,000 |
| `507566c5` (head) | same, sha `6935166c5663…` | 11 | **353** | **360** | **328** | 540 | 501 | not a simple fraction |
| `507566c5` | `safeguard_evaluation_results_llama.csv` sha `db321a9d4eee…` | 12 (adds Llama-Guard 4 12B, all-zero row) | 353 | 360 | 328 | 540 | 501 | not a simple fraction |

Denominators above are DERIVED: the smallest integer that makes every
safeguard's cell an integer count to 1e-7. Eleven independent rows reducing to
one denominator (or its divisors) is strong evidence, but any integer multiple
would also fit; see DERIVED for the test that removes this ambiguity for three
of the five supervisors.

### Reconciliation (no equality forced)

| number | where it lives | what the artifacts say |
|---|---|---|
| **990** = 330/330/330 | paper arXiv 2507.06282 §3, App. 0.B.2; `census.yaml` row `dataset_population`; MC-002 `scope`; disclosure page scope sentence | The paper's description of its non-adversarial set ("30 prompts per harm category", 11 categories). **No released artifact has this composition.** The 990 is the paper's text, not a file. |
| **1077** = 358/359/360 | upstream commit `d6ebd0e5` (2025-02-18), the second version of `non_adversarial_prompts.csv` | A 12-category pool including "Miscellaneous". Prompt Guard and LangKit columns are constant 1 (placeholders). Lakera/NeMo/LLM Guard columns are real verdicts (see DERIVED). |
| **1080** = 360/360/360 | upstream commit `0fc3d6d3` (v0) | Same pool plus 3 rows; same placeholders. |
| **353 / 360 / 328** (sum 1041) | inferred denominators of `safeguard_evaluation_results.csv` from `dde32a3d` through head; the aggregate the live leaderboard renders | **= file B minus its 36 Miscellaneous rows** (30 harmful + 6 benign; borderline has none). Verified exactly for Lakera, NeMo and LLM Guard in all three strata (nine of nine numerators match). 328 = eleven categories with two at 29. |
| **174** = 86/50/38 | commits `077555d9`–`ffe88ccb` (Feb 2025 playground) | First playground subset; superseded. |
| **170** = 82/50/38 | commits `b20aeed5` and `507566c5` (Mar–Jul 2025) | Current playground subset; the file MC-002/003/005 bind. A fresh draw from B (170/170 present), not a subset of the 174. |
| ~4,165 adversarial | census, MC-002 scope | Paper App. 0.E: narrative 3557 + syntactic 548 + generative 60 = **4165** exactly; no per-item release beyond the 8 rows of G (v0 history carried 114,540 adversarial rows, since deleted). |
| 990 vs 1041 | — | **Unreconciled and left so.** The paper describes 330 benign and 330 borderline; the computed aggregate uses 353 and 360. Harmful 328 vs 330 differs by the two 29-item categories. Which text is authoritative for "the evaluation's population" is UNKNOWN. |

## LIVE-CLAIM CHECK

Surfaces read: `index.html`, `/missing-column/`, `/missing-column/disclosure/`,
`/observatory/`, `/ledger/`, `/try/`, `/now/`, both `/answers/` pages,
`/corrections/`, `README.md`, `DESIGN.md`, `census.yaml`, `claims.yaml`,
`distribution/dossiers/bells-*.md`, `films/SLATE.md`, `films/LEDGER.md`,
`ARTIFACTS/12-WEEK-PROGRAM.md`.

| sentence (exact) | locator | class | requires live equivalence? |
|---|---|---|---|
| "On the 170-prompt per-item subset publicly released with BELLS's 2025 misuse-detection evaluation (pinned commit below), the five specialized supervisors … have an OR-union of released binary verdict columns that flags 73 of the 82 prompts labelled harmful" | `claims.yaml` MC-002 proposition; rendered `ledger/index.html:184`, `observatory/index.html:284` | explicit versioned measurement (commit + sha) | no |
| "Lakera Guard 63.4% (52 / 82) · Prompt Guard 6.1% (5 / 82) · LangKit 30.5% (25 / 82) · NeMo Guardrails 85.4% (70 / 82) · LLM Guard 0.0% (0 / 82)" under caption "The minimum joint disclosure computed on the released BELLS subset" | `missing-column/disclosure/index.html:251–256` | historical release description, present-tense table | no |
| "Counts describe the author-selected BELLS subset at its released native verdicts, not a vendor, a population, or a deployed stack" | `missing-column/disclosure/index.html:445` | explicit non-claim | no |
| "One supervisor fires exactly once in the whole released set." | `answers/what-does-the-second-guardrail-add/index.html:205`; MC-003 non_claims | generic present-tense prose about a file | no |
| "On the same released file, the five-supervisor union flags 19 of 50 benign prompts." | same page, line 214 | historical release description | no |
| "The BELLS 2025 misuse-detection evaluation published a 170-prompt subset with per-item verdicts for five specialized supervisors, so the joint behaviour can be recomputed rather than assumed." | `answers/why-guardrail-miss-rates-do-not-multiply/index.html:203–206` | historical release description | no |
| "the released verdicts are at unstated default configurations" | MC-002 and MC-003 non_claims | explicit limitation | no |
| "confirm that the released columns are the paper's supervisors at the paper's configurations (one yes/no)" | `distribution/dossiers/bells-misuse-2025.md` §5 | open question to the maintainer, held, unsent | no; it asks whether the *2025* columns match the *2025* paper |
| `thresholds_comparable: unstated — vendor defaults, binary verdicts, no calibration` | `census.yaml` row `bells-misuse-2025` | historical release description | no |
| `/now/` | `now/index.html` | names no supervisor | no |

Upstream's own dating: FAQ at `507566c5` — "We conducted evaluations in
January–February 2025 with limited resources and access"; paper §5 — "we did
not exhaustively test every API endpoint or product version; newer or alternate
configurations could yield different results."

**Result: no current product/API claim exists on any public surface of this
repository. Every sentence naming the five supervisors is either commit-pinned,
explicitly scoped to "the released file", or an explicit non-claim.**

## DECISION ON CUT 1

`CUT 1 VALUE = LOW / HISTORICAL SNAPSHOT ADEQUATELY BOUNDED` → **STOP**.

Two Cut 1 hard stops are already triggered by Cut 0, before any spend:

1. *Version identity unavailable.* The paper describes the released NeMo column
   as "NVIDIA's NeMo, using GPT-3.5 repurposed for supervision with a
   sophisticated prompt"; neither the rails configuration, the prompt, nor the
   GPT-3.5 snapshot is released. Lakera Guard's product version is likewise
   unstated. A 2026 mismatch could not be attributed to drift versus
   configuration.
2. *Endpoint semantics.* A live NeMo Guardrails call is a call to a framework
   the caller configures, not to the released supervisor. No frozen `h(raw)`
   would make the comparison the same object.

A 2026 API disagreement would not falsify file F; agreement would not convert
F into a population estimate. MC-002 stays non-population-level.

## OBSERVED

- Default branch of `CentreSecuriteIA/bells_leaderboard` is `main` at
  `507566c5` (GitHub branches API, 2026-09-05). No newer per-item release
  exists; `non_adversarial_prompts.csv` last changed at `b20aeed5` (2025-03-22).
- All 170 head questions are present in files A and B with identical
  `harm_level`; Lakera, NeMo and LLM Guard verdicts agree 82/82 on the harmful
  stratum between B (2025-02-18) and head (2025-07-08). Prompt Guard agrees on
  5/82 and LangKit on 25/82 because B carries placeholder 1s, not verdicts.
- The leaderboard aggregate's population changed once: `00b42bfd` (2025-02-21)
  used 359/360/358; `dde32a3d` (2025-02-23) and every later commit use
  353/360/328.
- The 170-row file shares only 74–76 questions with the 174-row playground
  files of February 2025.
- Upstream repository declares no licence (GitHub API `license: null`; no
  `LICENSE` in the tree at `507566c5`). MC-002 records "none declared
  upstream"; MC-005 at HEAD `f9b24e2` records `license: MIT` for the same file.
- The v0 commit `0fc3d6d3` carries 114,540 adversarial rows with prompt text,
  while the paper (App. 0.C) states the full dataset is not publicly released.
  Recorded as a provenance fact; nothing here redistributes it.
- The paper's Table 1 prints, for Lakera, "Detection Rate 77.8%" and
  "Adversarial Detection Rate 66.5%"; the aggregate file's Lakera
  `harmful_non-adversarial` is 0.6646 (218/328) and `harmful_jailbreaks` is
  0.7695. The same near-swap holds for NeMo, LangKit, Prompt Guard and LLM
  Guard.
- `python3 scripts/bells_denominators.py --cache …` exit 0; every pin hashed
  as recorded. `python3 scripts/reanalyze_bells_subset.py` output cached in
  `_private/…/reanalyze_bells_subset.stdout.txt` reproduces MC-002 (not
  re-run here; the working tree's uncommitted `exclusive_cells` block belongs
  to the owner's in-progress change).
- `python3 scripts/verification_manifest.py` on this working tree: 45 of 46
  gates pass; the "acquisition surfaces" gate (`scripts/verify_growth.py`)
  fails with 159 findings, every one of them on pages under the gitignored,
  untracked `_private/bets/disclosure-source-cache/` directory (created
  2026-09-05 00:34, before this session), which `verify_growth.pages()` reaches
  through `ROOT.rglob("index.html")`. Zero findings on tracked files. A clean
  clone has no `_private/`, so CI is unaffected; the two files this packet
  adds are not `index.html` and are not touched by that gate.
- Working tree (untouched): staged removal of MC-005 from `claims.yaml`
  relative to HEAD; unstaged addition of `exclusive_cells` to MC-002 and a
  matching `claims_history.yaml` transition; modified generators, verifiers,
  film receipts; untracked `experiments/e3/`.

## DERIVED

- Aggregate population identification: removing the 36 `Miscellaneous` rows
  from file B leaves 328 harmful / 353 benign / 360 borderline, and the counts
  of released-1 verdicts in those rows equal the aggregate's numerators for
  Lakera (218, 40, 104), NeMo (281, 63, 233) and LLM Guard (2, 0, 1). Nine of
  nine match. Therefore the leaderboard's non-adversarial rates for those three
  supervisors are computed on exactly the verdicts in file B minus
  Miscellaneous, and the 82-row harmful stratum of file F is a subset of that
  population carrying the same verdicts.
- The same test cannot be run for Prompt Guard and LangKit: file B holds
  placeholders for them, and no per-item release of their population verdicts
  exists. Their aggregate numerators (15/328, 1/353, 6/360 and 77/328, 26/353,
  20/360) are consistent with the 353/360/328 denominators but unverifiable
  per item.
- 4165 = 3557 + 548 + 60 (paper App. 0.E), so the census's "~4,165" is exact.
- File F's five-guard marginals (52/82 etc.) differ from the aggregate's
  (218/328 etc.) because the denominators differ; neither is an estimate of the
  other. MC-002's numbers are not the leaderboard's numbers and were never
  claimed to be.

## UNKNOWN

- The rule that selected the 170 (and earlier 174) playground rows from pool B.
  `SELECTION RULE: UNKNOWN`.
- Which of 990 (paper text) and 1041 (computed aggregate) the authors regard as
  "the" non-adversarial population, and why benign is 353 rather than 330.
- Whether the paper's Table 1 column headers are swapped relative to the
  aggregate file's stratum columns, or the printed values come from a
  different (rebalanced or per-category-averaged) computation. Not resolved
  here; not this repository's claim.
- Product versions and configurations of all five supervisors at measurement
  time (January–February 2025 per the FAQ).
- The rule that removed 3 rows between files A and B.
- Why the harmful-jailbreak aggregate cells at `dde32a3d` and later are not
  simple fractions (the paper's rebalancing across jailbreak types is the
  likely mechanism; not verified).

## STRONGEST COUNTER

The identification in DERIVED is less interesting than it looks: it shows the
82 released harmful verdicts for three supervisors are the population verdicts,
which was already the natural reading of the census row and of MC-002's
"released binary verdict columns". It adds a provenance chain, not a new
number, and it says nothing about whether those verdicts were ever right. The
still-missing objects — the selection rule for the 170 and the population
verdicts for Prompt Guard and LangKit — are exactly the two the dossier already
names as its smallest asks. Cut 0 therefore sharpens an existing ask; it does
not create a finding.

## FALSIFIER

- A byte change at any pinned (commit, path) pair fails
  `scripts/bells_denominators.py` and voids the corresponding table row.
- A public statement by the BELLS authors that the aggregate was computed on a
  population other than file B minus Miscellaneous would refute the DERIVED
  identification; the nine exact matches would then be coincidence, which is
  the falsifier's burden to explain.
- Discovery of any public sentence on this site that asserts a current rate
  for Lakera Guard or NeMo Guardrails, without a commit pin or a
  "released file" scope, reopens the Cut 1 decision and must be quoted with
  its locator before any spend.
- A per-item release newer than `b20aeed5` on the upstream default branch
  fires MC-002's `remote_content_change` trigger and supersedes this packet's
  "no newer release" statement.

## NON-CLAIMS

- Nothing here estimates any supervisor's true rate, in 2025 or now.
- Nothing here says the leaderboard's numbers are wrong; it says which
  population they were computed on.
- The 990/1041 discrepancy is recorded, not adjudicated; no repository text
  was changed to prefer either.
- No E2 item, threshold, estimator or criterion was touched. E2 remains
  `UNTESTED BY DESIGN`; P2 remains `EMPTY`.
- No live API was called; no money was spent; nobody was contacted.
- MC-002, MC-003, MC-005 and the census row were not edited. Two candidate
  owner actions are noted, not performed: (a) MC-005's `license: MIT` is
  unsupported by upstream (no licence declared) and conflicts with MC-002's
  record for the same file; (b) the census row's evidence text "170 prompts
  … the full dataset is available only by contacting the authors" is
  incomplete in one respect — a 1077-row per-item file with three real
  supervisor columns exists in upstream history at `d6ebd0e5`. Neither
  changes N/M/K, the row's PRESENT classification, or any MC-002 count.
  `census.yaml` carries MC-001's sha trigger, so any edit is an owner
  re-review, not a packet edit.

## REPRODUCTION

```bash
# Denominator / provenance table, every upstream byte hash-asserted (~2 MB download):
python3 scripts/bells_denominators.py
python3 scripts/bells_denominators.py --json      # machine-readable
python3 scripts/bells_denominators.py --cache DIR # offline after first run

# The joint arithmetic on file F (MC-002), unchanged:
python3 scripts/reanalyze_bells_subset.py

# The lineage as one figure, drawn from the --json output above (counts only, no rates):
python3 scripts/bells_lineage_figure.py        # writes ARTIFACTS/2026-09-05-bells-denominator-lineage.svg
```

The `--json` output also carries `aggregate_identification`: the DERIVED test
above run mechanically (pool `d6ebd0e5` minus Miscellaneous versus the
aggregate's numerators; 9/9 for the three non-placeholder supervisors).

Pins (commit → path → sha256 prefix), all at `https://raw.githubusercontent.com/CentreSecuriteIA/bells_leaderboard/<commit>/<path>`:
`0fc3d6d3` non_adversarial `b3d5553510f0` · `d6ebd0e5` non_adversarial `a8d2e4ea69b3` ·
`077555d9` non_adversarial `7fa0fbf5885e` · `00b42bfd` non_adversarial `52ca9ab8eb62` ·
`ffe88ccb` non_adversarial `d93f9fe1d1a1` · `b20aeed5`/`507566c5` non_adversarial
`791dd4b0a168f2eb5831b308083a492e83200a9fa82585643c739023b03f57c3` ·
`507566c5` adversarial `32fe8663621a` · `0fc3d6d3` aggregate `06eeb0251e5b` ·
`00b42bfd` aggregate `d906e69c1fd8` · `dde32a3d` aggregate `6c133bea489b` ·
`507566c5` aggregate `6935166c5663204fc23feb9385d1801cd69436d7bb95df5e92cbb77bdfdab84c` ·
`507566c5` aggregate_llama `db321a9d4eee` · `507566c5` metacognitive `66a9f09c60d3`.

Upstream commit dates and subjects: GitHub commits API,
`/repos/CentreSecuriteIA/bells_leaderboard/commits?path=data/non_adversarial_prompts.csv`
and `…/commits/<sha>`, read 2026-09-05. Paper text: `https://arxiv.org/html/2507.06282`
(App. 0.B.2, 0.C, 0.D, 0.E; §5 Limitations; Table 1). Site FAQ: `faq.html @ 507566c5`.

The new script is not wired into `scripts/verification_manifest.py`; adding a
47th gate is the owner's call, and it would introduce a second network
dependency on the same upstream.

STATE: STOP
